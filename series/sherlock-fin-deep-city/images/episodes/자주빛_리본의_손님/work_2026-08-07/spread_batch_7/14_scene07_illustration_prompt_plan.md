# 자줏빛 리본의 손님 — 내지 14페이지 장면 07 그림 계획

## 범위

- 내지 14페이지: 장면 07 그림 전용 왼쪽 페이지.
- 목표 후보: `14_scene07_illustration_candidate_v1_hangul_board.png`.
- 맞은편 예정 글 페이지: `15_페이지.png`.
- 상태: `candidate v1 generated — user review pending`.

## 공식 입력 레퍼런스

- `references/characters/셜록핀.png`: 셜록 핀 외형·복장·꼬리·돋보기.
- `references/characters/펄리.png`: 펄리 외형·눈 반짝이는 표정·정지 시 아래 조개 접지.
- `references/locations/웃고_있는데_슬픈_얼굴_탐정사무소_내부_레퍼런스.png`: 조개 책상, 청록 단서 칠판, 둥근 창, 물방울 램프.
- `references/props/자줏빛_리본의_손님_핵심단서_소품_레퍼런스.png`: 자모 카드 8장과 완성 글자 카드 디자인.
- 이전 생성 후보를 입력하지 않고 공식 레퍼런스 4장만 사용한다.

## 장면 핵심

- 산호 골목에서 탐정 사무소로 돌아온 직후.
- 청록색 추리 칠판을 화면 정면의 가장 큰 요소로 배치한다.
- 셜록 핀은 칠판 옆에서 첫 묶음을 가리키며 조립 과정을 설명하고, 펄리는 안정된 낮은 받침 위에서 눈을 반짝이며 칠판을 본다.
- 학습 순서와 글자가 인물 장식보다 먼저 읽혀야 한다.

## 칠판 글자 잠금

- 허용되는 문자·글자는 정확히 다음뿐이다: `ㅁ`, `ㅣ`, `ㅇ`, `ㅕ`, `ㄱ`, `ㅅ`, `ㅜ`, `ㅍ`, `미`, `역`, `숲`, `미역숲`.
- 첫 칸: 청록 낱장 `ㅁ` 오른쪽에 청록 낱장 `ㅣ` → 완성 `미`.
- 둘째 칸: 금색 낱장 `ㅇ` 오른쪽에 금색 낱장 `ㅕ`, 그 아래 금색 낱장 `ㄱ` → 완성 `역`.
- 셋째 칸: 보라 낱장 `ㅅ` 아래에 보라 낱장 `ㅜ`, 그 아래 보라 낱장 `ㅍ` → 완성 `숲`.
- 아래 결론 칸: `미` + `역` + `숲` 순서가 이어져 정확히 `미역숲`.
- 자모 8장은 각각 독립된 네 모서리·색 테두리·사이 틈을 가진 낱장 카드다. 긴 합판·공용 테두리·붙은 카드 금지.
- 각 조립 단계는 얇은 화살표 또는 `+` 모양 기호로만 연결한다. 추가 한글·영문·숫자·가짜 글자·문장 금지.

## 구도·신체 잠금

- 칠판은 정면에 가깝고 원근 왜곡이 적으며, 모든 카드가 손·돋보기·인물에 가리지 않는다.
- 셜록 핀과 펄리는 모두 칠판을 바라본다. 독자 정면 응시 금지.
- 셜록 핀은 한 손으로 첫 단계를 가리키고 연속된 인어 꼬리를 유지한다. 다리·발·신발 금지.
- 펄리는 위·아래 조개가 같은 축에 정렬되고 아래 조개 전체가 낮은 조개 의자·책상 받침에 접지한다. 별도 하반신·다리·발 금지.
- 정확히 두 인물만 등장한다.

## 아트스타일·질감

- 공식 시트의 2D 선화·큰 눈·제한된 부드러운 명암·매끈한 색면을 따른다.
- 3D 토이·클레이·픽사풍·플라스틱 광택을 금지한다.
- 칠판 주변의 기존 메모·실·영문 간판을 제거하고 학습 카드만 남긴다.
- 점묘·잔기포·잔자갈·반복 반짝이·미세 표면무늬를 억제한다.
- 조명은 따뜻하되 칠판 글자 대비를 낮추지 않는다.

## QA

- `ㅁ+ㅣ=미 / ㅇ+ㅕ+ㄱ=역 / ㅅ+ㅜ+ㅍ=숲 / 미역숲` 조립이 정확한가.
- 자모 카드 8장이 모두 분리된 낱장인가.
- 다른 글자·영문·가짜 문자가 없는가.
- 두 인물의 시선이 칠판으로 향하고 신체 구조가 공식 시트와 일치하는가.
- 사무소와 배경의 미세 질감이 억제되었는가.

## 후보 v1 결과

- 생성 원본: `C:/Users/yurib/.codex/generated_images/019fdc33-d58d-79f2-a486-33638838b7db/exec-0527c5bc-da06-4a33-88b2-c5ee035c9a7f.png`.
- 작업 원본: `14_scene07_illustration_candidate_v1_hangul_board_raw.png`.
- 검토 후보: `14_scene07_illustration_candidate_v1_hangul_board.png`.
- 한글 QA: `ㅁ + ㅣ → 미`, `ㅇ + ㅕ + ㄱ → 역`, `ㅅ + ㅜ + ㅍ → 숲`, 최종 `미역숲`이 정확함.
- 카드 QA: 자모 8장이 각각 완전한 네 모서리·독립 색 테두리·사이 틈을 가진 낱장으로 분리됨. 합판·연결판 없음.
- 추가 문자 QA: 허용된 자모·`미·역·숲·미역숲` 외 한글·영문·숫자·가짜 글자 없음.
- 칠판 QA: 정면에 가까운 청록 칠판이 화면의 가장 큰 요소이며 모든 카드가 손·돋보기·인물에 가리지 않음.
- 캐릭터 QA: 셜록 핀은 칠판을 가리키고 펄리는 눈을 반짝이며 칠판을 봄. 연속된 인어 꼬리, 펄리 조개 정렬·받침 접지, 다리·발·신발 없음.
- 장소·저질감 QA: 공식 사무소의 조개 칠판·둥근 창·물방울 램프를 유지하면서 기존 메모·실·영문 장식과 미세 배경 요소를 제거함.
- 규격 QA: 생성 원본 1024×1536 보존, 검토 후보 1054×1492 PNG.
- 파일 QA: 후보 2,325,489 bytes, SHA-256 `985B4D117615A69BB740A99D958A8618FDABD20564EC8E45DD8E5A11FE0AEEDD`.
- 상태: `candidate v1 generated — user review pending`. 승인 전 `final/14_페이지.png`로 승격하지 않는다.
- 사용자 승인: `좋아`.
- 최종 파일: `final/14_페이지.png`.
- 후보·최종본 SHA-256: `985B4D117615A69BB740A99D958A8618FDABD20564EC8E45DD8E5A11FE0AEEDD`, 해시 일치.
- 상태: `approved — promoted to final/14_페이지.png`.
